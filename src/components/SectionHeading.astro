---
interface Props { eyebrow?: string; title: string; }
const { eyebrow, title } = Astro.props;
---
<div class="section-heading">{eyebrow && <p class="eyebrow">{eyebrow}</p>}<h2>{title}</h2><span class="gold-rule" aria-hidden="true"></span><slot /></div>
